<?xml version="1.0" encoding="utf-8"?>
<!-- KMDF driver project for asb_ivshmem. Build: MSBuild /p:Configuration=Release /p:Platform=ARM64.
     Requires the WDK + its VS integration (VSIX). Spectre mitigation is OFF for this test build
     (the Spectre libs aren't installed; mitigation is a hardening flag, not needed to function).
     KMDF version pinned to 1.33 to match asb_ivshmem.inf's KmdfLibraryVersion. -->
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemGroup Label="ProjectConfigurations">
    <ProjectConfiguration Include="Release|ARM64">
      <Configuration>Release</Configuration>
      <Platform>ARM64</Platform>
    </ProjectConfiguration>
  </ItemGroup>
  <PropertyGroup Label="Globals">
    <ProjectGuid>{9F1B25AD-3EBA-4111-8B37-B92AAFA2547F}</ProjectGuid>
    <RootNamespace>asb_ivshmem</RootNamespace>
    <MinimumVisualStudioVersion>17.0</MinimumVisualStudioVersion>
    <TargetVersion>Windows10</TargetVersion>
    <DriverType>KMDF</DriverType>
    <Configuration>Release</Configuration>
    <Platform>ARM64</Platform>
  </PropertyGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />
  <PropertyGroup Label="Configuration">
    <TargetVersion>Windows10</TargetVersion>
    <UseDebugLibraries>false</UseDebugLibraries>
    <PlatformToolset>WindowsKernelModeDriver10.0</PlatformToolset>
    <ConfigurationType>Driver</ConfigurationType>
    <DriverType>KMDF</DriverType>
    <SpectreMitigation>false</SpectreMitigation>
  </PropertyGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />
  <PropertyGroup>
    <KMDFVersionMajor>1</KMDFVersionMajor>
    <KMDFVersionMinor>33</KMDFVersionMinor>
  </PropertyGroup>
  <ItemDefinitionGroup>
    <ClCompile>
      <WarningLevel>Level4</WarningLevel>
      <TreatWarningAsError>false</TreatWarningAsError>
    </ClCompile>
  </ItemDefinitionGroup>
  <ItemGroup>
    <ClCompile Include="asb_ivshmem.c" />
  </ItemGroup>
  <ItemGroup>
    <Inf Include="asb_ivshmem.inf" />
  </ItemGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
</Project>
